# Invalid Flowchart - Uses Actor Shape

diagram "flowchart"

# This should fail validation because flowchart doesn't allow actor shapes
shape user as @actor label: "User"
shape process as @rounded label: "Process"
shape decision as @rhombus label: "Decision"

user -> process
process -> decision